<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Palavra do Dia</title>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #6a11cb, #2575fc);
            color: white;
            text-align: center;
            padding: 20px;
        }
        .container {
            background: rgba(255, 255, 255, 0.15);
            border-radius: 20px;
            padding: 30px;
            max-width: 500px;
            backdrop-filter: blur(10px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.3);
        }
        h1 {
            font-size: 2rem;
            margin-bottom: 10px;
        }
        .word {
            font-size: 2.5rem;
            font-weight: bold;
            margin: 15px 0;
        }
        .definition {
            font-size: 1.1rem;
            opacity: 0.9;
        }
        .date {
            margin-top: 15px;
            font-size: 0.9rem;
            opacity: 0.7;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>📖 Palavra do Dia</h1>
        <div class="word" id="word"></div>
        <div class="definition" id="definition"></div>
        <div class="date" id="date"></div>
    </div>

    <script>
        const palavras = [
            { palavra: "Resiliência", definicao: "Capacidade de se adaptar e se recuperar diante de dificuldades ou mudanças." },
            { palavra: "Efêmero", definicao: "Que dura pouco tempo, passageiro, transitório." },
            { palavra: "Procrastinar", definicao: "Adiar ou deixar para depois uma tarefa ou obrigação." },
            { palavra: "Altruísmo", definicao: "Atitude de ajudar os outros sem esperar nada em troca." },
            { palavra: "Perspicaz", definicao: "Capaz de perceber e compreender as coisas com clareza e rapidez." },
            { palavra: "Inócuo", definicao: "Que não causa dano ou prejuízo, inofensivo." },
            { palavra: "Ímpar", definicao: "Singular, único, extraordinário." }
        ];

        function getWordOfTheDay() {
            const today = new Date();
            const dayOfYear = Math.floor(
                (today - new Date(today.getFullYear(), 0, 0)) / 86400000
            );
            return palavras[dayOfYear % palavras.length];
        }

        function exibirPalavra() {
            const palavraDia = getWordOfTheDay();
            document.getElementById("word").textContent = palavraDia.palavra;
            document.getElementById("definition").textContent = palavraDia.definicao;

            const hoje = new Date().toLocaleDateString("pt-BR", {
                weekday: "long",
                day: "numeric",
                month: "long",
                year: "numeric"
            });
            document.getElementById("date").textContent = hoje;
        }

        exibirPalavra();
    </script>
</body>
</html>
