<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Palavra do Dia</title>
<style>
    body {
        font-family: Arial, sans-serif;
        background: #121213;
        color: white;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        height: 100vh;
        margin: 0;
        padding-top: 20px;
    }
    h1 {
        margin-bottom: 20px;
    }
    #grid {
        display: grid;
        grid-template-columns: repeat(5, 60px);
        grid-gap: 10px;
        margin-bottom: 20px;
    }
    .cell {
        width: 60px;
        height: 60px;
        border: 2px solid #3a3a3c;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2rem;
        font-weight: bold;
        text-transform: uppercase;
        background: #121213;
        color: white;
    }
    .correct {
        background: #538d4e; /* verde */
        border-color: #538d4e;
    }
    .present {
        background: #b59f3b; /* laranja */
        border-color: #b59f3b;
    }
    .absent {
        background: #3a3a3c; /* cinza */
        border-color: #3a3a3c;
    }
    input {
        width: 150px;
        padding: 10px;
        font-size: 1.2rem;
        text-transform: uppercase;
        text-align: center;
    }
    button {
        padding: 10px 20px;
        font-size: 1rem;
        margin-left: 10px;
        cursor: pointer;
    }
    #mensagem {
        margin-top: 20px;
        font-size: 1.2rem;
    }
</style>
</head>
<body>

<h1>Palavra do Dia</h1>

<div id="grid"></div>

<div>
    <input type="text" id="entrada" maxlength="5" autofocus>
    <button onclick="verificar()">Tentar</button>
</div>

<div id="mensagem"></div>

<script>
    const palavras = ["CASAL", "JOGOS", "TREVO", "MOUSE", "LINHA", "CARRO", "BOLA", "PRAIA", "NAVIO"];
    const data = new Date();
    const indiceDia = Math.floor(data.getTime() / (1000 * 60 * 60 * 24)) % palavras.length;
    const palavraDoDia = palavras[indiceDia];
    
    const tentativasMax = 6;
    let tentativaAtual = 0;
    let grid = document.getElementById("grid");

    for (let i = 0; i < tentativasMax * 5; i++) {
        let cell = document.createElement("div");
        cell.classList.add("cell");
        grid.appendChild(cell);
    }

    function verificar() {
        let entrada = document.getElementById("entrada");
        let palpite = entrada.value.toUpperCase();
        if (palpite.length !== 5) {
            alert("Digite uma palavra com 5 letras!");
            return;
        }

        let startIndex = tentativaAtual * 5;

        for (let i = 0; i < 5; i++) {
            let cell = grid.children[startIndex + i];
            cell.textContent = palpite[i];

            if (palpite[i] === palavraDoDia[i]) {
                cell.classList.add("correct");
            } else if (palavraDoDia.includes(palpite[i])) {
                cell.classList.add("present");
            } else {
                cell.classList.add("absent");
            }
        }

        tentativaAtual++;
        entrada.value = "";

        if (palpite === palavraDoDia) {
            document.getElementById("mensagem").textContent = "🎉 Você acertou!";
            document.getElementById("entrada").disabled = true;
        } else if (tentativaAtual >= tentativasMax) {
            document.getElementById("mensagem").textContent = `❌ Fim de jogo! A palavra era ${palavraDoDia}`;
            document.getElementById("entrada").disabled = true;
        }
    }
</script>

</body>
</html>
